[Sleep]
# Suspend configuration - use deep sleep (S3) for better wake reliability
SuspendMode=suspend
SuspendState=mem

# Hibernate configuration  
HibernateDelaySec=120s
HibernateMode=shutdown
HibernateState=disk

# Suspend-then-hibernate - wait longer before hibernating
SuspendEstimationSec=60min